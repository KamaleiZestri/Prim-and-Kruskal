import re
from Weighted_Graph import Weighted_Graph as wg

def minCostEdge(graph):
    #Returns the edge with the minimum weight within a given graph.
    

    
def connectTrees(temp,graph,tree):
    #Determines if the edge given connects two previously unconnected verticies in a given tree.
    #Returns true if so
    
    
    